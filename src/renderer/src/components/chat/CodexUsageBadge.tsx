/**
 * CodexUsageBadge — minimal stub.
 *
 * The original implementation was a LOCAL, UNCOMMITTED file removed during a
 * workspace reset (`git clean`) and is not recoverable from git. This stub
 * restores compilation; it renders nothing. Restore the real component from a
 * local backup if the usage/quota badge is needed.
 */
export interface CodexUsageBadgeProps {
  providerId?: string
}

export function CodexUsageBadge(_props: CodexUsageBadgeProps) {
  return null
}

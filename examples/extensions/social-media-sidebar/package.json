{
  "name": "@Rcode-examples/social-media-sidebar",
  "version": "0.1.3",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "validate": "node ../run-repository-Rcode-cli.mjs extension validate .",
    "pack": "npm run build && node ../run-repository-Rcode-cli.mjs extension pack . --overwrite"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "vite": "^6.2.0",
    "vitest": "^4.1.7"
  }
}

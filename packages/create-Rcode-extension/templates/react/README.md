# {{DISPLAY_NAME}}

Generated Rcode Node host + sandboxed React Webview extension for Extension API
1.x. React runs only in the extension Webview, never inside Rcode's React tree.

```sh
npm install
npm test
npm run validate
npm run pack
```

Developer documentation: https://Rcode.dev/extensions/1/
